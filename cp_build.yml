name: currency
image: currency


build:
  script: ./build.sh
  docker:
    enable: true
    dockerFile: ./back-end/

test:
  stages:
    unit:
      junit: back-end/target/surefire-reports
#  script:
#  alt_image_builder: jib
#  secrets:
#    - sjaksl
#    - ashkjsaas
#    - shasjksa
