name: currency
image: currency
images:
  - currencyv1
  - currencyv2

build:
  script: ./build.sh
  docker:
    enable: true
    dockerFile: ./back-end/

test:
  stages:
    unit:
      script: mvn test -f back-end/pom.xml
      junit: back-end/target/surefire-reports
#  script:
#  alt_image_builder: jib
#  secrets:
#    - sjaksl
#    - ashkjsaas
#    - shasjksa
